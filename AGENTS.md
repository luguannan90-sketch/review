# 铁电材料项目文件输出约定

- 本项目后续生成的文件统一放入 `output/` 目录。
- 包括分析结果、表格、图像、报告、脚本运行产物、临时导出文件和其他新生成的交付物。
- 已存在的原始 PDF 和参考资料不移动、不覆盖，除非用户明确要求。
- 生成文件需要使用清晰、可区分的文件名；不同任务可在 `output/` 下继续建立子文件夹。
- 生成完成后，报告中说明输出文件位于 `output/` 下的具体路径，并进行必要的打开、读取或格式检查。
